@model AutoYahtzee.Business.ViewModels.YahtzeeListViewModel
@{
    ViewData["Title"] = Consts.PageTitle.Stats;
}

<div class="resources-page">
    <partial name="PageTitle" for="@Consts.PageTitle.Stats" />

    <div class="row">
        <div class="col-md-12">
            <p>
                The probability, <strong class="math-symbol">P</strong>, of rolling a <strong class="math-symbol">d</strong>-dice Yahtzee within <strong class="math-symbol">r</strong> rolls can be expressed as:
            </p>
            <img class="mx-auto d-block formula-img" src="~/img/formula.gif" alt="Yahtzee probability formula" />
        </div>
    </div>

    <div class="row">
        <div class="col-md-12">
            <div style="height:500px;">
                <canvas id="probability-chart"></canvas>
            </div>
        </div>
    </div>
</div>